export interface ProfileAttributes {
    id: String,
    email: String,
    password: String,
    name: String,
}

export interface ItemAttributes {
    id: String,
    item: String,
    vaultNumber: String,
}
